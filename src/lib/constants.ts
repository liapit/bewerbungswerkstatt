// ── Hero CV Stream Settings ──

/** Number of unique cards per conveyor row (doubled internally for seamless loop) */
export const CV_STREAM_CARD_COUNT = 16;

/** Base animation duration in seconds for the forward row */
export const CV_STREAM_SPEED = 45;

/** Base animation duration in seconds for the reverse row */
export const CV_STREAM_SPEED_REVERSE = 50;

/** Playback rate multiplier when hovering a conveyor row */
export const CV_STREAM_HOVER_SPEED = 3;

/** Probability (0–1) that any single card is highlighted */
export const CV_STREAM_HIGHLIGHT_CHANCE = 0.1;
